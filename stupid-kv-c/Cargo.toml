[package]
name = "stupid-kv-c"
version = "0.0.10"
edition = "2021"
publish = false
license = "MIT"
description = "C ABI FFI bindings for stupid-kv (consumed by the Go bindings via cgo)"

[lib]
name = "stupid_kv_c"
crate-type = ["cdylib", "staticlib"]

[dependencies]
stupid-kv = { path = "../" }
